You are given an integer n. Print the first n even numbers.

Sample Input:

7

Sample Output:

2 4 6 8 10 12 14

function firstNEvenNumbers(n) {
    let num = '';
    for (let i = 1; i <= n; i++) {
        num += (i * 2) + ' ';
    }
    console.log(num.trim());
}
